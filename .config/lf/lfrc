set drawbox
set icons
set previewer pistol

cmd mkdir ${{
  printf "Directory name: "
  read answer
  mkdir $answer
}}

map <delete> delete
map <enter> open
map <esc> quit

map i up
map j updir
map k down
map l open

map h top
map ";" bottom
map m mkdir
map o $$PAGER "$f"
